//import packages
import express from 'express';
//create a route
const router = express.Router();

/*
    Create a Session
    Remove a Session
    Mute a User
    Unmute a User
*/

export default router;